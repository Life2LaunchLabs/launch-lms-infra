server {
  listen 8080;
  location /api/ { proxy_pass http://control-plane-api:8000; proxy_set_header Host $host; proxy_set_header X-Forwarded-Proto $scheme; }
  location / { root /usr/share/nginx/html; try_files $uri /index.html; }
}
